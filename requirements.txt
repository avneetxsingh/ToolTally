# ── ToolTally requirements ────────────────────────────────────────
# Install on the Pi with:
#     pip install -r requirements.txt
#
# Pillow + opencv-python are typically already present on Raspberry Pi
# OS; keep them here for a clean fresh-install flow.

# Core
numpy
opencv-python
Pillow

# Supabase client
supabase

# PCA9685 + Servo (Adafruit CircuitPython stack)
# These pull in adafruit-blinka, board, busio, etc. automatically.
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor
adafruit-blinka
